/**
 * Expense Tracker — App Entry Point
 * Phase 1: Icon initialization only.
 * Full logic implemented in Phase 2+.
 */

document.addEventListener('DOMContentLoaded', function () {
  if (typeof lucide !== 'undefined') {
    lucide.createIcons();
  }
});
